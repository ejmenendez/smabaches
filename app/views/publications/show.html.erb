
<h1>
	<strong>T&iacute;tulo:</strong>
	<%= @publication.title %>
</h1>

<h3>
	Autor: <%= @publication.author.email%>
</h3>

<p>
	<strong>Descripci&oacute;n:</strong>
	<%= @publication.description %>
</p>

<p>
<strong>Image:</strong>
<div class="col-md-4">
		<%= image_tag @publication.photo.url :class => "slide-image"%>
</div>
</p>
<div class="clearfix">

</div>
<div claas="col-md-6">
  <div id="map" style="width: 400px;height:250px"></div>
</div>

<script>
	handler = Gmaps.build('Google');
	handler.buildMap({ provider: {}, internal: {id: 'map'}}, function(){
	  markers = handler.addMarkers((<%=raw @hash.to_json %>));
	  handler.bounds.extendWith(markers);
		handler.map.centerOn([-40.1552557,-71.3472031])
		handler.getMap().setZoom(14);
	});
</script>

<div class="clearfix">

</div>
<div class="btn-group">
	<% if policy(@publication).edit? %>
		<%= link_to 'Editar', edit_publication_path(@publication), :class => "btn btn-default" %>
	<% end %>

	<% if policy(@publication).destroy? %>
		<%= link_to 'Borrar', publication_path(@publication), method: :delete, :class => "btn btn-default"  %>
	<% end %>

	<%= link_to 'Volver al índice', publications_path, :class => "btn btn-default"  %>
</div>
